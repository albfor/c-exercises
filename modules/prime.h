#ifndef PRIME_H
#define PRIME_H

/**
 * Check if a number is a prime number.
 *
 * @number The number to check if it is prime.
 * @returns Returns true if number is a prime, false if it is not.
 */
bool isprime(unsigned int number);

#endif
